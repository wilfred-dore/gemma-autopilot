[project]
name = "gemma-autopilot"
version = "0.1.0"
description = "Gemma 4 optimizes its own deployment — an autonomous, explainable inference-tuning agent"
requires-python = ">=3.10"
license = { text = "Apache-2.0" }
dependencies = [
    "pynvml",
    "codecarbon",
    "requests",
]

[project.optional-dependencies]
dev = ["pytest"]
